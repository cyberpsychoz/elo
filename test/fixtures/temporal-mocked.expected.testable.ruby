(raise "Assertion failed" unless Klang.today == Date.parse('2025-12-01'); true)
(raise "Assertion failed" unless Klang.today + 1 == Date.parse('2025-12-02'); true)
(raise "Assertion failed" unless Klang.today - 1 == Date.parse('2025-11-30'); true)
(raise "Assertion failed" unless Klang.today.beginning_of_week == Date.parse('2025-12-01'); true)
(raise "Assertion failed" unless Klang.today.beginning_of_month == Date.parse('2025-12-01'); true)
(raise "Assertion failed" unless Klang.today.beginning_of_quarter == Date.parse('2025-10-01'); true)
(raise "Assertion failed" unless Klang.today.beginning_of_year == Date.parse('2025-01-01'); true)
(raise "Assertion failed" unless Klang.today.beginning_of_week <= Klang.today; true)
(raise "Assertion failed" unless Klang.today.end_of_week >= Klang.today; true)
(raise "Assertion failed" unless Klang.today.beginning_of_month <= Klang.today; true)
(raise "Assertion failed" unless Klang.today.end_of_month >= Klang.today; true)
(raise "Assertion failed" unless Klang.today.beginning_of_quarter <= Klang.today; true)
(raise "Assertion failed" unless Klang.today.end_of_quarter >= Klang.today; true)
(raise "Assertion failed" unless Klang.today.beginning_of_year <= Klang.today; true)
(raise "Assertion failed" unless Klang.today.end_of_year >= Klang.today; true)
