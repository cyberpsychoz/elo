(SELECT x + INTERVAL 'P1D' FROM (SELECT CURRENT_DATE AS x) AS _let)
(SELECT CURRENT_DATE + d FROM (SELECT INTERVAL 'P1D' AS d) AS _let)
(SELECT (SELECT x + d FROM (SELECT INTERVAL 'P1D' AS d) AS _let) FROM (SELECT CURRENT_DATE AS x) AS _let)
